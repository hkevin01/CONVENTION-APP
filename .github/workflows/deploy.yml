name: Deploy

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Install dependencies
        run: npm install
      # Add deployment steps here, e.g., upload to hosting, Expo publish, etc.
      - name: Deploy step
        run: echo "Add deployment commands here"
